<div class="jumbotron text-center">
  <h1 class="display-4">Bienvenue sur EventLand</h1>
  <p class="lead">Découvrez et créez des événements inoubliables près de chez vous.</p>
  <hr class="my-4">
  <p>Rejoignez notre communauté et participez à des événements exceptionnels.</p>
  <a class="btn btn-success btn-lg" href="<%= new_event_path %>" role="button">Créer un événement</a>
</div>

<div class="container">
  <div class="row">
    <% @events.each do |event| %>
      <div class="col-md-4">
        <div class="card mb-4 shadow-sm">
          <div class="card-body">
            <h5 class="card-title"><%= event.title %></h5>
            <p class="card-text"><%= truncate(event.description, length: 100) %></p>
            <p><strong>Lieu :</strong> <%= event.location %></p>
            <p><strong>Date :</strong> <%= l(event.start_date, format: :long) %></p>
            <a href="<%= event_path(event) %>" class="btn btn-info">Voir l'événement</a>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
