<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <title><%= content_for(:title) || "Event Land" %></title>

    <%= stylesheet_link_tag "bootstrap.min", "application", "data-turbo-track": "reload" %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootswatch/5.1.3/vapor/bootstrap.min.css">
    <link rel="stylesheet" href="path/to/your/custom.css"> <!-- Votre fichier CSS personnalisé -->

    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <!-- Nom de l'application -->
            <a class="navbar-brand" href="#">Mon Application</a>

            <div class="collapse navbar-collapse">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <%= link_to "Accueil", root_path, class: "nav-link active" %>
                    </li>
                    <li class="nav-item">
                        <%= link_to "Créer un événement", new_event_path, class: "nav-link" %>
                    </li>
                </ul>
                
                <ul class="navbar-nav ms-auto">
                    <% if user_signed_in? %>
                        <li class="nav-item dropdown">
                            <%= link_to "Mon Profil", user_path(current_user), class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= form_with url: destroy_user_session_path, method: :delete, class: "nav-link" do %>
                                <%= submit_tag "Se déconnecter", class: "dropdown-item" %>
                            <% end %>
                        </li>
                    <% else %>
                        <li class="nav-item">
                            <%= link_to "S'inscrire", new_user_registration_path, class: "nav-link" %>
                        </li>
                        <li class="nav-item">
                            <%= link_to "Se connecter", new_user_session_path, class: "nav-link" %>
                        </li>
                    <% end %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-5">
        <%= yield %>
    </div>

    <!-- Scripts -->
    <script src="https://js.stripe.com/v3/"></script>
</body>

<footer class="footer bg-light text-center py-3">
  <div class="container">
    <p class="text-muted mb-1">© 2025 Mon Application. Tous droits réservés.</p>
    <ul class="list-inline">
      <li class="list-inline-item"><a href="#" class="link-secondary">À propos</a></li>
      <li class="list-inline-item"><a href="#" class="link-secondary">Contact</a></li>
      <li class="list-inline-item"><a href="#" class="link-secondary">Politique de confidentialité</a></li>
    </ul>
  </div>
</footer>
</html>
